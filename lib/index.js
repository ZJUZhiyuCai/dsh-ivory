/**
 * Ivory has no host-side behavior. Keeping this entry point inert means the
 * theme does not need filesystem, network, process, or web-server access.
 */
export const name = 'dsh-ivory';

export function apply() {}
